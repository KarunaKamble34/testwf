on: 
  workflow_dispatch:
    inputs:
      alphabet:
        type: string
        default: abc
        required: false
  
jobs:
   first-job:
     runs-on: windows-latest
     steps:
       - run: node --version 
       - run: npm --version
